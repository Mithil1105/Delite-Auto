# <Feature Name>

> Copy this file to create documentation for a new feature or change. Keep every section —
> write "N/A" rather than deleting a section that doesn't apply. This file is read by both
> human engineers and AI assistants/reviewers, so be explicit and avoid assuming unstated context.

## Metadata

| Field          | Value                                   |
|----------------|------------------------------------------|
| Feature name   |                                          |
| File           | `Documentations MD/<slug>.md`           |
| Branch         |                                          |
| Owner          |                                          |
| Status         | Draft / In Progress / Done / Deprecated |
| Created        | YYYY-MM-DD                              |
| Last updated   | YYYY-MM-DD                              |

## Summary

One to three sentences: what is this feature/change, in plain language.

## Why

The motivation or problem this solves. Link to any request, issue, or decision that drove it.
This is the section most likely to matter later — code shows *what*, this section should
carry *why*.

## Scope

What is included in this change, and explicitly what is **not** included (so reviewers don't
assume more happened than did).

## Implementation notes

How it was built: key files/modules touched, important design decisions, non-obvious tradeoffs,
anything a reviewer would otherwise have to reverse-engineer from the diff.

- File: `path/to/file.ts` — what changed and why
- File: `path/to/other.tsx` — what changed and why

## Interfaces / data

Any new or changed props, functions, API endpoints, data shapes, or config. Skip if none.

## Dependencies

New packages, environment variables, or other features/branches this depends on. Skip if none.

## Testing / verification

How this was verified — manual steps taken, commands run, what was checked in the browser,
automated tests added. Be concrete enough that another engineer (or AI) could re-verify it.

## Known issues / follow-ups

Anything intentionally left unfinished, deferred, or known-broken. Skip if none.

## Revision log

| Date       | Author | Change                                  |
|------------|--------|------------------------------------------|
| YYYY-MM-DD |        | Initial version                          |
